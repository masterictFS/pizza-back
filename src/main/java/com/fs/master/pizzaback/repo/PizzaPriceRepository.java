package com.fs.master.pizzaback.repo;

import com.fs.master.pizzaback.model.PizzaPrice;
import org.springframework.data.repository.CrudRepository;

public interface PizzaPriceRepository extends CrudRepository<PizzaPrice, Long> {
}
